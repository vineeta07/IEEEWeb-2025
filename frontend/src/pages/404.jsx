import boilerPlate from '@/layout/Boilerplate'
import React from 'react'
import NotFound from '@/utils/NotFound'

function NotFoundPage() {
  return (
    <div> <NotFound /></div>
  )
}

export default boilerPlate(NotFoundPage);
